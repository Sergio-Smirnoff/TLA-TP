"+" -> PLUS;
"-" -> MINUS;
"*" -> TIMES;

Regex digit         [0-9];

{digit}+ -> State var {
                       var.attribute.num = Integer.parseInt(var.getLexeme());
                       return NUMBER;
 };

[ \t\n\r]            /* skip whitespace */;

default -> { return REJECT; };
